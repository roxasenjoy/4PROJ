{% extends 'auth/login.html.twig' %}

{% block title %}Password Reset Email Sent{% endblock %}

{% block content_body %}
    <p class="resetPasswordSend">
        Si un compte correspondant à votre adresse e-mail existe, un e-mail vient d'être envoyé contenant un lien
        que vous pouvez utiliser pour réinitialiser votre mot de passe. Ce lien expirera dans
        {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
    </p>


    <p class="resetPasswordSend" style="margin-bottom: 25px">Si vous ne recevez pas d'e-mail, veuillez vérifier votre dossier spam ou <a href="{{ path('app_forgot_password_request') }}">réessayer</a>.</p>



    <div class="action-login">
        <a href="{{ path('app_login') }}" style="margin-top: 25px">
            <button>Se connecter</button>
        </a>
    </div>

{% endblock %}
